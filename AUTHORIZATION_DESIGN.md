# MissionOS 项目与任务权限认证设计

> 状态：设计稿（未开始实现）
> 适用范围：项目列表、任务列表、项目详情、任务操作、组织授权及权限管理页面
> 目标：在不改动 Multica 原生用户、工作区成员和任务归属约束的前提下，提供独立、可配置、可审计的项目级权限。

## 1. 边界与不变量

权限认证必须建立在统一登录后的 MissionOS `user_id` 上，与钉钉、邮箱或其他登录来源无关。

```text
认证（Authentication）
  当前请求是谁？
        ↓
组织目录（Organization）
  用户属于哪些租户、部门和组织？
        ↓
项目授权（Authorization）
  用户在项目/任务上能做什么？
```

必须保持以下不变量：

1. 任务必须绑定项目，不能创建或授权一个没有项目的任务。
2. 任务默认继承所属项目对应角色的完整权限矩阵，不是只继承 `View`。
3. 可以给单个任务增加直接授权，但直接授权不能绕过项目存在性、工作区归属和最低项目访问边界。
4. 没有项目访问权的用户，即使被 @ 或设置为负责人，也不能因为任务直授而获得项目其他任务或项目详情权限。
5. 项目负责人、任务负责人是业务字段，不等于自动获得工作区全部项目权限。
6. 权限判断必须在后端执行；前端隐藏按钮只能改善体验，不能作为安全边界。

## 2. 独立模块边界

建议继续使用独立包 `server/pkg/projectauth`，通过仓储接口连接数据库，避免把权限 SQL 和规则散落到 Multica 原生处理器中。

```text
HTTP Handler / WebSocket
          ↓ Subject + resource ID + action
     projectauth.Service
          ├─ Scope（列表可见范围）
          ├─ CheckProject（项目权限）
          ├─ CheckIssue（任务权限）
          └─ Report（权限报告）
          ↓
   ProjectAuthRepository
          ↓
  grants / roles / organization directory
```

权限模块不负责登录、部门同步、任务创建业务和项目负责人字段；这些模块通过稳定接口提供用户和组织信息。

## 3. 角色与权限模型

### 3.1 工作区角色

工作区角色继续继承 Multica 原生成员表：`owner`、`admin`、`member`。工作区角色用于识别工作区级管理边界，不复制一套用户或成员体系。

当前约定：

- `owner`：工作区级最高管理者，可管理项目授权和角色定义；
- `admin`：继续保留 Multica 原生管理能力，但不是项目授权的自动替代品；
- `member`：只有显式项目授权或组织/全员授权才能访问项目。

### 3.2 项目角色

系统角色可配置权限，初始角色建议为：

| 角色 | 默认权限 |
| --- | --- |
| Owner | 查看、编辑、创建任务、管理任务、使用 Agent、管理项目成员、管理项目设置 |
| Manager | 查看、编辑、创建任务、管理任务、使用 Agent |
| Member | 查看、创建任务、使用 Agent |
| Viewer | 查看 |

权限键使用稳定字符串，便于新增能力而不改变表结构：

```text
project.view
project.edit
project.issue.create
project.issue.manage
project.agent.use
project.member.manage
project.settings.manage
```

角色权限不是写死在前端。系统角色提供安全默认值，工作区 Owner 可在设置中调整；自定义角色只能使用后端允许的权限键。

## 4. 授权主体与授权表

授权主体统一抽象为四类：

| 主体类型 | 示例 | 适用场景 |
| --- | --- | --- |
| user | 单个 MissionOS 用户 | 精确授权某个人 |
| role | 项目角色 | 角色权限矩阵 |
| organization | 部门/组织节点 | 按钉钉、企微、飞书或其他 OA 部门授权 |
| everyone | 当前工作区全员 | 全员查看或其他受控权限 |

建议以独立的 `projectauth_access_grants` 作为统一授权事实表，至少包含：

```text
id
workspace_id
project_id
issue_id（为空表示项目级，非空表示任务级）
subject_type / subject_id
role_key 或 permission
source（manual、organization、everyone、migration）
created_by / created_at / updated_at
```

唯一性应覆盖同一资源、主体和角色/权限，写入使用数据库唯一约束保证幂等。

当前代码仍使用 `project_members` 和 `issue_permissions` 读取项目/任务授权。迁移到统一授权表时不要直接删除旧表：先回填、双读或桥接，再切换写入，最后在确认通知、负责人和历史接口没有依赖后下线旧表。负责人字段不迁移为成员关系；它仍是项目/任务原生字段。

## 5. 有效权限计算

对用户 `u`、项目 `p`、任务 `i` 和权限 `x`，有效权限按以下顺序计算：

```text
1. 校验登录身份、工作区归属和资源存在性
2. 工作区 Owner：按工作区策略直接允许
3. 计算用户在项目上的所有有效授权：
   - user 直接授权
   - 所属 organization 授权
   - everyone 全员授权
   - 项目角色对应的 permission 集合
4. 任务请求：先确认用户具备项目最低访问权，再合并任务级直接授权
5. 任一来源允许 x，则允许；没有允许来源则拒绝
```

任务级直接授权只扩大指定任务的权限范围：

- 不会使用户看到项目下的其他任务；
- 不会自动获得项目设置、项目成员管理等项目级权限；
- 不会绕过项目与工作区归属校验；
- 任务上授予 `project.edit` 也只作用于该任务的编辑操作。

如果产品需要显式拒绝（deny），应在引入前定义优先级和审计语义；第一版建议只使用 allow grant，避免组织授权与个人拒绝产生不可解释的结果。

## 6. 列表与详情的可见范围

### 项目列表

项目列表只返回当前用户具有项目可见权限的项目。空间 Owner 的项目可见范围可以通过页面开关选择：

- 包含空间 Owner 自动可见的项目；
- 仅显示显式项目授权、组织授权或全员授权的项目。

该开关只影响展示范围，不改变后端权限事实，也不改变项目操作权限。

### 任务列表

任务列表必须同时满足：

1. 任务属于当前工作区中的一个有效项目；
2. 当前用户拥有该项目的最低查看权限，或拥有该任务的受约束直接授权；
3. 任务级直接授权只返回被授权的具体任务，不扩展为项目全量任务。

空间 Owner 可以使用同样的“包含/隐藏空间权限任务”展示开关。关闭后仍可通过明确的任务 ID 访问自己有明确授权的任务，但不能通过列表查询绕过筛选。

### 详情与操作

详情接口和写操作都必须重新执行 `CheckProject`/`CheckIssue`。不能因为任务列表已经过滤，就省略任务详情接口的鉴权。

## 7. 授权管理流程

项目创建时支持直接选择授权主体和角色；创建后可在项目详情或设置中的“项目权限”弹窗增删授权。

建议流程：

1. 授权人打开项目权限弹窗；
2. 选择用户、组织或全员主体；
3. 选择角色；
4. 后端校验授权人具备 `project.member.manage`；
5. 事务写入 grant 并返回当前有效权限；
6. 列表和详情缓存失效；
7. 记录授权人、主体、角色、项目/任务和变更时间。

角色定义页面只允许工作区 Owner 修改角色权限。项目 Owner 可以使用角色，但不能修改影响整个工作区的角色定义。

## 8. 与组织适配器的关系

组织目录只提供标准关系：

```text
tenant → organization → member(user_id)
```

钉钉、企微、飞书等适配器负责同步到组织目录；权限模块只读取标准组织关系，不读取第三方 API。组织同步延迟期间，使用最近一次成功快照并记录同步状态。

因此，新增登录方式或新增 OA 不需要修改项目权限计算器，只需要实现登录/组织适配器并将外部人员绑定到同一个 MissionOS `user_id`。

## 9. 安全、性能与兼容性要求

- 所有权限判断在后端完成，统一返回 401（未登录）、403（无权限）、404（资源不存在或不属于当前工作区）。
- 不把完整项目权限塞进长期 JWT；权限变更后应立即生效。
- 列表使用批量权限查询，避免每个项目/任务执行一次 SQL。
- 授权写入使用事务和唯一约束，重复点击必须幂等。
- 组织授权变更、角色修改和全员授权均写审计日志。
- `PROJECT_PERMISSION_ENABLED=false` 时保留现有 Multica 行为，便于灰度；开启但迁移未完成时应 fail closed 并返回明确配置错误。
- 权限 SQL 保持在仓储适配器中，`projectauth` 核心包只依赖接口，降低从上游拉取更新时的冲突面。

## 10. 实施顺序与验收

1. 保留现有 `projectauth.Service` 和原生路由，补齐统一 grant 仓储接口。
2. 建立组织目录适配器和 `organization/everyone` 主体解析。
3. 回填 `project_members`、`issue_permissions` 到统一授权表，先双读验证结果一致。
4. 修正任务授权边界：没有项目最低访问权时，任务直接授权也不得访问；直接授权只覆盖指定任务。
5. 将项目/任务列表、详情和写操作全部接入同一权限服务。
6. 增加权限报告：按项目、任务、人员、角色展示授权来源和有效权限。
7. 稳定运行后再评估旧表下线，不能以删除旧表作为第一步。

验收至少覆盖：

- 用户、组织、全员三种授权来源的合并结果；
- 项目角色权限可配置且变更立即生效；
- 任务继承项目的完整权限矩阵；
- 任务直接授权不会扩大到其他任务；
- 无项目权限的负责人或被 @ 用户不能访问任务；
- 空间 Owner 的列表显示开关只影响列表范围，不改变实际授权；
- 项目创建、项目权限弹窗、角色设置和任务详情均有可用入口；
- 旧数据迁移前后权限结果可通过对账查询验证。
